<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- WebView pages (privacy / support) need the network. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Haptic feedback on block seat / collapse. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".TowerBreakApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TowerBreak">

        <!--
          Single-activity architecture: one host activity, all screens are Compose
          destinations under Navigation Compose. Orientation is NOT fixed here —
          the splash unlocks rotation and the playfield locks to portrait at
          runtime (see RotationLock), mirroring the original per-screen behaviour.
          configChanges keeps us from recreating the activity on rotation.
        -->
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|density|uiMode|locale|layoutDirection|fontScale"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.TowerBreak.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
